# Python 3.14 · Windows 기준으로 검증된 조합.
# 설치:  pip install --only-binary=:all: -r requirements.txt
#   --only-binary 을 붙이는 이유: scikit-network 최신 버전은 소스 배포만 있어
#   C++ 컴파일러가 필요하다. 휠이 있는 버전으로 물러나게 강제한다.
#
# ⚠️ langchain 계열의 상한(<1.0)을 풀지 말 것.
#   1.x 세대에서는 langchain-community 가 chat_models.vertexai 를 제거해
#   ragas 가 import 단계에서 실패한다. Day 1~7 실습 코드도 0.3 세대 기준이다.

# ── 과정 공통 스택 (Amazon Bedrock) ────────────────────────
langchain>=0.3,<1.0
langchain-core>=0.3,<1.0
langchain-community>=0.3,<0.4
langchain-aws>=0.2,<1.0
langchain-text-splitters>=0.3,<1.0
langgraph>=0.2,<1.0
langgraph-checkpoint-sqlite<3.0
python-dotenv>=1.0
pydantic>=2.7
boto3>=1.34

# ── RAG ────────────────────────────────────────────────────
chromadb>=0.5
langchain-chroma<1.0
rank-bm25>=0.2
kiwipiepy>=0.17

# ── API (POST /query) ──────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30

# ── 평가 · 관측 ────────────────────────────────────────────
ragas==0.2.15
langsmith>=0.1
pytest>=8.4
