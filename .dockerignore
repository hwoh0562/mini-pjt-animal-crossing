# 자격증명 — 이미지에 절대 들어가면 안 된다.
# 실행 시 docker run --env-file .env 로 주입한다.
.env
.env.*

# 가상환경 · 빌드 산물
.venv/
venv/
__pycache__/
*.py[cod]

# 런타임 생성물 — 컨테이너 안에서 새로 만든다
chroma_db/
*.sqlite
*.sqlite-*

# 저장소 메타데이터
.git/
.gitignore

# 교육 과정 내부 자료
mini PJT.pdf
*.zip

# 개발 문서 (실행에 불필요)
CLAUDE.md
