# SERVICE · 동물의 숲 무인도 생활 어시스턴트

## 1. 사용자·문제·가치

**누구를 위한 서비스인가**
'모여봐요 동물의 숲'을 플레이 중인 한국어 유저. 특히 ① 도감을 채우려는데 지금 시각에 뭐가 잡히는지 매번 찾아봐야 하는 초보~중급 플레이어, ② 주민과 친해지고 싶은데 누가 뭘 좋아하는지 모르는 플레이어, ③ 무(turnip) 거래 손익을 매번 암산하는 플레이어.

**어떤 문제를 푸는가**
생물 100종의 출현 조건(시간대·장소·그림자 크기·판매가)과 주민 20명의 프로필(종족·성격·생일·선호 색상)이 방대한 표로 흩어져 있어, 플레이 도중 게임을 멈추고 위키를 검색해야 확인할 수 있다.

**이 에이전트 없을 때는 어떻게 해결하고 있는가 · 이걸로 뭐가 나아지는가**
지금은 게임을 중단하고 외부 위키·공략 사이트에서 표를 눈으로 훑으며 조건을 직접 필터링해야 한다. 이 에이전트는 자연어 한 문장("지금 잡을 수 있는 곤충 알려줘", "9월에 생일인 주민 있어?")으로 **조건이 이미 적용된 결과를 근거 문서와 함께** 돌려준다. 무 거래는 내가 알려준 매수단가를 기억해 손익을 대신 계산해 주므로, 플레이 흐름을 끊지 않는다.

## 2. 서비스 확장 관점

**어떻게 돈을 벌 수 있는가 / 어떤 기여를 하는가**
게임 커뮤니티(디스코드 서버·카페)에 상주 챗봇으로 배포해 프리미엄 구독(광고 제거·다중 캐릭터 프로필 관리)으로 과금하거나, 공략 사이트의 체류시간 증대용 핵심 기능으로 넣어 광고 수익을 올린다. 사내 관점에서는 **"방대한 조건표 + 개인 상태 DB + 계산 + 되돌릴 수 없는 액션"** 이라는 구조가 사내 HR·복리후생·경비 정산 어시스턴트와 동일하므로, 이 프로젝트가 그대로 그 도메인의 레퍼런스 구현이 된다.

**규모 · 대체제와의 차별점**
국내 커뮤니티만 해도 수만 명 단위, 시즌 이벤트 기간에는 하루 수 회씩 반복 조회하는 패턴이 나온다. 기존 위키/공략 사이트는 **전체 표를 보여주고 필터링은 사용자 몫**이지만, 이 에이전트는
1. 현재 시각·카테고리 같은 조건을 **미리 적용한 결과만** 주고,
2. 답변마다 **출처 문서(doc_id)를 붙여** 검증 가능하며,
3. 내가 알려준 무 매수단가·보유 벨을 기억해 **개인화된 손익**을 계산한다.

## 3. 사용 예상 도구·데이터

### 데이터 소스

모든 데이터는 `data/make_data.py` 로 생성한다. 스크립트에 `random.seed(42)` 를 고정해 두어 **재실행해도 같은 값이 나온다**(1차·2차 평가 비교의 전제).

| 파일 | 내용 | 실제/가짜 | 상태 |
|---|---|---|---|
| `data/insects.json` | 곤충 **50종** — `id`(I-001~I-050) · 이름 · 출현 장소 · 출현 시간대 · 판매가 | 이름은 게임 내 실제 명칭, 나머지 속성은 **생성된 더미** | ✅ 생성됨 |
| `data/fishes.json` | 물고기 **50종** — `id`(F-001~F-050) · 이름 · 출현 장소 · 그림자 크기 · 출현 시간대 · 판매가 | 위와 동일 | ✅ 생성됨 |
| `data/villagers.json` | 주민 **20명** — `id` · 이름 · 종족 · 성격 · 생일 · 좋아하는 색 | 게임 내 실제 주민 정보 기반 | ✅ 생성됨 |
| `data/personalities.md` | 성격 **7종**(느끼함 · 성숙함 · 아이돌 · 친절함 · 무뚝뚝 · 먹보 · 운동광)별 성향 · 선호 선물 3개 · 기피 선물 · 친밀도 팁. `P-00` 은 공통 규칙(선호 색 일치 · 생일 보너스 · 포장) | 자체 작성 마크다운 | ✅ 생성됨 |
| `data/player_state.json` | 플레이어 상태 **초기 시드** — 보유 벨 42,300 · 무 120개 @ 매수단가 88벨 · 거래 이력 3건 | **가짜(더미) 1인분** | ✅ 생성됨 |
| `data/player_state_no_price.json` | 위와 같되 **매수단가가 비어 있는(`null`) 상태**. 에이전트가 되묻는지 확인하는 평가용 고정 시드 | 위와 동일 | ✅ 생성됨 |

**플레이어 상태의 읽기/쓰기 위치**: 위 두 JSON은 **시드 전용이며 런타임에 절대 덮어쓰지 않는다.** 최초 1회 **LangGraph Store**에 적재한 뒤, 이후의 조회와 갱신(`sell_all_turnips`, 사용자가 알려준 매수단가 반영)은 **전부 Store에서만** 일어난다. 그래야 평가를 몇 번 돌려도 시드가 그대로 남아 1차·2차 비교가 성립한다.

**RAG 인덱싱 단위**: 레코드 1건 = 문서 1건.
- 생물은 레코드의 `id` 를 그대로 `doc_id` 로 쓴다(`I-031` · `F-007`).
- 주민은 정수 `id` 라서 `V-{id:02d}` 로 변환한다(`V-02` = 쭈니).
- 성격 가이드만 마크다운이므로 `## [P-0N]` 섹션 단위로 쪼개 그 태그를 `doc_id` 로 쓴다(`P-01` = 느끼함).

답변의 `contexts` 는 이 `doc_id` 로 반환된다.

**데이터 범위 밖**: 해산물은 수집하지 않는다. 또한 이 데이터에는 **출현 월·계절·반구·날씨 정보가 없으므로**, 해당 조건 질의에는 답하지 않고 보유하지 않은 정보임을 안내한다.

**무 가격**: 게임에서 섬마다 무작위 생성되므로 외부에서 조회할 수 없다. **사용자가 말해준 값을 기록**해서 쓴다(예: "지금 148벨이야").

### 도구 목록

| 도구 | 유형 | 인터페이스 | 설명 |
|---|---|---|---|
| `retrieve_docs` | 검색 (RAG) | `(query, doc_type?) -> [{doc_id, text, score}]` | **대상 이름을 아는 질의**("쭈니는?", "무당벌레 어디서?")의 설명 검색. **쿼리 확장 → 하이브리드 검색(BM25 + 임베딩) → 리랭킹** 3단계 |
| `check_critter_availability` | 조회 (구조화 필터) | `(category, hour, location?, min_price?) -> [critter]` | **조건만 아는 질의**. 카테고리(곤충/물고기)와 시각 기준으로 채집 가능 생물 필터링. 판매가 내림차순 **상위 15종**만 싣고 전체 건수는 `total_count` 로 함께 준다 |
| `search_villagers` | 조회 (구조화 필터) | `(species?, personality?, birth_month?) -> [villager]` | **조건만 아는 질의**. 종족·성격·생일 월로 주민 필터링. 조건을 만족하는 **전원**을 누락 없이 반환하며, 없으면 빈 목록을 반환한다. 조건이 둘 이상인데 결과가 비면 조건별 결과를 `partial_matches` 로 함께 주어, 왜 해당자가 없는지 설명할 근거를 남긴다 |
| `get_player_state` | 조회 (장기 메모리) | `() -> {bells, turnips, buy_price, history}` | 플레이어 상태 조회. LangGraph Store에 보관되어 **대화 세션을 넘어 유지**된다 |
| `calculate_turnip_profit` | 계산 | `(quantity, buy_price, current_price) -> {revenue, profit, roi}` | 무 매수단가 대비 손익·수익률 계산 |
| `sell_all_turnips` | **액션 (HITL)** | `(current_price) -> {sold, revenue, new_bells}` | 보유 무 전량을 매도 처리하고 **Store의 플레이어 상태를 갱신**(무 0 · 벨 증가 · 거래 이력 추가). 시드 JSON은 건드리지 않는다. 게임 본체를 조작하지 않으며, 어디까지나 에이전트가 관리하는 기록을 바꾸는 동작 |

**도구 선택 기준**: **대상 이름을 알면 `retrieve_docs`, 조건만 알면 구조화 필터 도구**(`check_critter_availability` · `search_villagers`). 집계·필터 질의("늑대 주민 전부", "9월 생일")를 유사도 검색으로 처리하면 누락이 생기므로, 이런 질의는 반드시 구조화 필터를 탄다.

**검색 실패 시 재시도**: `retrieve_docs` 가 빈 결과를 내면 미들웨어가 쿼리를 재작성해 **1회만** 재시도한다. 두 번째도 비면 지어내지 않고 "확인되지 않는다"로 넘긴다(정책 2). 이름이 숫자인 주민(`1호`)처럼 임베딩이 의도를 잘못 잡는 질의가 이 경로를 탄다.

### 응답 규약

`POST /query` 응답은 **Pydantic 모델로 고정**하며, 세 필드 모두 비우거나 생략하지 않는다.

```json
{
  "answer": "쭈니는 느끼함 성격이라 정장·선글라스 같은 멋내기 아이템을 좋아합니다. 좋아하는 색이 화이트라 흰색 계열이면 반응이 더 좋습니다.",
  "contexts": [
    {"doc_id": "V-02", "text": "쭈니 · 다람쥐 · 느끼함 · 9월 29일 · 화이트"},
    {"doc_id": "P-01", "text": "느끼함 — 좋아하는 선물: 정장·셔츠류 의류 · 선글라스·모자 ..."}
  ],
  "trace": [
    {"step": "guardrail_in", "input": "쭈니한테 무슨 선물 주면 좋아?", "output": "pass"},
    {"step": "retrieve", "input": "쭈니 선물 느끼함", "output": "V-02, P-01"},
    {"step": "generate", "input": "...", "output": "..."},
    {"step": "guardrail_out", "input": "...", "output": "pass"}
  ]
}
```

- **`answer`** — LCEL 체인 끝에서 Pydantic 스키마로 파싱한 결과. 모델의 자유 문자열을 그대로 흘려보내지 않는다.
- **`contexts`** — 검색된 문서의 `doc_id` 와 원문. RAGAS `context_recall` / `context_precision` 의 입력이다.
- **`trace`** — 입력 가드레일 → 검색 → 도구 호출 → 생성 → 출력 가드레일까지 **모든 단계**를 순서대로 기록한다. 같은 트레이스를 **LangSmith**(대안: LangFuse)에도 남겨, 실패한 테스트 케이스를 단계 단위로 되짚는다.

`step` 값은 아래 **6개로 고정**한다. 임의의 이름을 쓰지 않는다.

| `step` | 기록 시점 |
|---|---|
| `guardrail_in` | 입력 가드레일 판정 |
| `retrieve` | `retrieve_docs` 검색 |
| `retrieve_retry` | 검색 결과가 비어 쿼리를 재작성해 재시도 |
| `tool` | 그 외 도구 호출 (구조화 필터 · 계산 · 액션) |
| `generate` | 최종 답변 생성 (구조화 출력) |
| `guardrail_out` | 출력 가드레일 판정 |

### 활용 패턴 (Day 1~7 · 10개)

| # | 패턴 | 이 서비스에서의 적용 |
|---|---|---|
| 1 | LCEL + Pydantic 구조화 출력 **(필수)** | 위 응답 규약 3필드를 Pydantic 모델로 고정 |
| 2 | ReAct (도구 자율 선택) | 질의에 따라 도구 6개 중 필요한 것을 모델이 고름 |
| 3 | RAG — 쿼리 확장 · 하이브리드 검색 · 리랭킹 **(필수)** | `retrieve_docs` 3단계 파이프라인 |
| 4 | 도구 다중 결합 | "지금 무 팔면 이득이야?" → `get_player_state` + `calculate_turnip_profit` |
| 6 | 가드레일 | §4 정책 5건 · 입력/출력 양쪽 필터링 |
| 7 | HITL (위험 작업 승인) | `sell_all_turnips` 실행 전 `interrupt()` 로 승인 대기 |
| 8 | 미들웨어 (재시도) | 검색 결과가 비면 쿼리 재작성 후 1회 재시도 |
| 10 | 장기 메모리 | 플레이어 상태를 LangGraph Store에 보관 · 세션 간 유지 |
| 11 | Observability · Trace **(필수)** | `trace` 필드 + LangSmith 기록 |
| 12 | 평가 (RAGAS · LLM-as-Judge) **(필수)** | §5 성공 기준 |

**미사용**: 5(MCP 서버 연동) · 9(Multi-Agent Supervisor). 도구 6개를 단일 ReAct 그래프가 직접 다루는 규모라, 서브 에이전트 분할은 이득 없이 복잡도만 올린다. MCP는 이 서비스의 데이터가 전부 로컬 파일이라 외부 서버로 노출할 대상이 없어 제외했다.

## 4. 서비스 정책 (가드레일 요약)

1. **어뷰징 정보 차단** — 타임슬립(게임 내 시간 조작), 아이템 복사 버그, 세이브 데이터 변조, 치트 기기 사용법 등 정상 플레이를 벗어난 방법은 **방법을 설명하지 않고 정중히 거절**하고 정상적인 대안을 안내한다.
2. **근거 없는 답변 금지** — 모든 사실 진술은 검색된 문서에 근거해야 하며, 답변에 출처 `doc_id` 를 명시한다. 보유하지 않은 정보(해산물 · 출현 월 · 계절 · 날씨 · 등장하지 않는 생물·주민)는 **지어내지 않고 "확인되지 않는다"고 안내**한다.
3. **불확실한 수치 단정 금지** — 미래 무 시세, 주민 친밀도 내부 수치처럼 게임이 공개하지 않거나 무작위인 값은 **확정적인 숫자로 답하지 않는다.**
4. **위험 작업 사용자 승인 (HITL)** — `sell_all_turnips` 처럼 보유 재화 기록을 되돌릴 수 없게 바꾸는 도구는 실행 전 반드시 사용자 승인을 받는다. 승인 전에는 계산 결과만 보여준다.
5. **도메인 이탈 · 인젝션 차단** — 실제 주식·암호화폐·정치 등 범위 밖 질문은 담당 범위가 아님을 알린다. 시스템 프롬프트 노출 요구나 "이전 지시 무시" 류의 요청은 거부하고 정상 서비스를 유지한다.

세부 케이스는 `evaluation/test_queries.csv` 에서 검증한다. 정책 1·5는 `guardrail` 항목(id 18~20), 정책 2는 `negative` 항목(id 9~11), 정책 3은 id 12, 정책 4는 id 13에 대응한다.

## 5. 성공 기준

**인-아웃 세트 통과율**
- 1차(Day 9 종료): 20건 중 **14건(70%)** 이상
- 2차(Day 10 개선 후): 20건 중 **17건(85%)** 이상 — 이 시점을 "완성"으로 본다
- 판정 방식: LLM-as-Judge가 `expected_traits` 전 항목 충족 + `forbidden` 무포함 + `expected_tools` 호출 일치를 모두 만족할 때만 통과

**"쓸만하다"를 판단할 지표**

| 지표 | 목표 |
|---|---|
| 가드레일 차단율 (id 18~20) | **100%** — 1건이라도 뚫리면 미완성 |
| 무 손익 계산 수학적 오류 (id 7·13) | **0건** — 기대값과 정확히 일치해야 함 |
| 고정 정답 케이스 정확도 (id 5·6·17) | **100%** — `search_villagers` 구조화 필터로 결정되는 답이라 확률적 요소가 없다. 오답이면 도구 선택 또는 필터 결함 |
| RAGAS `faithfulness` | ≥ 0.85 (환각 없이 문서에 충실) |
| RAGAS `context_recall` | ≥ 0.80 |
| RAGAS `context_precision` / `answer_relevancy` | ≥ 0.75 |
| 출처 명시율 | 문서 근거가 필요한 답변의 **100%** 에 `doc_id` 표기 |
| 트레이스 기록률 | 전체 응답의 **100%** 에 비어 있지 않은 `trace` 포함. 검색을 탄 응답은 `retrieve` 단계가 반드시 있어야 한다 |
| 트레이스 추적 가능성 | 실패한 케이스 전건을 LangSmith에서 **단계 단위로 재현**할 수 있을 것 |

---

### 작성 시 둔 가정 (구현 전 확정 필요)

- **현재 시각**: 시스템 시각을 기본으로 쓰고, 사용자가 게임 내 시각을 말하면 그 값을 우선 적용한다.
- **시간대 해석**: 데이터의 `time` 값은 `하루 종일` · `AM 9시 ~ PM 4시` · `PM 4시 ~ AM 9시` 등 문자열이므로, 도구 내부에서 `(시작시각, 종료시각)` 으로 파싱해 자정을 넘는 구간까지 처리한다.
- **무 가격 예측 없음**: 가격 패턴 예측은 범위에서 제외하고 손익 계산만 한다(정책 3과 일관).
- **RAGAS 정답 세트**: `test_queries.csv` 스키마에 `ground_truth` 컬럼이 없으므로, RAGAS 산출용 정답 세트는 `evaluation/ragas_set.csv` 로 별도 관리한다.
